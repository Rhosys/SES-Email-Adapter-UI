// Tailwind v4 is configured via CSS (`@theme` in src/assets/main.css).
// This file exists for editor IntelliSense and to scope content discovery.
export default {
  content: ['./index.html', './src/**/*.{vue,ts}'],
  theme: { extend: {} }
};
